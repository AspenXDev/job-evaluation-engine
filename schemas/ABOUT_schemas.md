This directory contains JSON schemas that define the structure of system inputs and outputs.

Schemas enforce consistency and validation.

Examples:

evaluation-output.schema.json
user-profile-schema.json
