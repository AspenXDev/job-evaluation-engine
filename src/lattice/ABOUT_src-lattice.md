This module implements the Lattice Scoring Model.

The Lattice model evaluates jobs across multiple
capability dimensions.

Core components:

dimension_definitions
scoring_functions
aggregation
lattice_model

These modules together compute the final job capability
score.

dimension_definitions -> nodes
aggregation -> topology
lattice_model -> orchestration
